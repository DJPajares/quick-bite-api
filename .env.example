# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/quick-bite-api

# Business Configuration
TAX_RATE=0.08
SERVICE_FEE_RATE=0.05
SESSION_TIMEOUT_MINUTES=120

# JWT Authentication
# Generate a secure secret key using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# IMPORTANT: Keep this secret and never commit it to version control
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Legacy Admin Token (Deprecated - use JWT authentication instead)
# ADMIN_TOKEN=admin-secret-token
